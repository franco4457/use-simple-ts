export interface {{capitalize hookName}}Props {}

export function {{camelCaseTS hookName}}(props: {{capitalize hookName}}Props = {}) {
  const { ...otherProps } = props

  return { ...otherProps }
}

export type {{capitalize hookName}}Return = ReturnType<typeof {{camelCaseTS hookName}}>
