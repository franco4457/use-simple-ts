import { renderHook } from '@testing-library/react'

import { {{camelCaseTS hookName}} } from '../src'

describe('{{camelCaseTS hookName}}', () => {
  it('should work correctly', () => {
    const { result } = renderHook(() => {{camelCaseTS hookName}}())

    // Add your test here
  })
})
